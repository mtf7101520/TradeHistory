//@version=5
strategy("动态调整的卖出策略_CMF优化版", overlay=true, format=format.price, precision=2, calc_on_every_tick=false)

//普通均线绘制 start
// 设置EMA线
ema1 = ta.ema(close, 144)
ema2 = ta.ema(close, 169)
ema3 = ta.ema(close, 12)
ema5 = ta.ema(close, 26)
ema6 = ta.ema(close, 9)

// Define channel colors
buyColor = ema3 > ema1 or ema3 > ema2 ? color.new(color.green, 0) : na
sellColor = ema3 < ema2 or ema3 < ema1 ? color.new(color.red, 0) : na

buy2Color = ema6 > ema5 or ema6 > ema3 ? color.new(color.green, 0) : na
sell2Color = ema6 < ema3 or ema6 < ema5 ? color.new(color.red, 0) : na

// Plot the channel
plot(ema1, color=color.black, linewidth=1)
plot(ema2, color=color.purple, linewidth=1)
plot(ema3, color=color.rgb(176, 39, 39), linewidth=2)
plot(ema5, color=color.rgb(0, 0, 0), linewidth=1)
plot(ema6, color=color.rgb(255, 82, 82), linewidth=1)
fill(plot(ema1), plot(ema2), color=buyColor)
fill(plot(ema2), plot(ema1), color=sellColor)
fill(plot(ema3), plot(ema5), color=buy2Color)
fill(plot(ema5), plot(ema3), color=sell2Color)
//普通均线绘制 end

// 动态买卖参数
sellPrecent = input.int(1, title="sellPrecent 基数0.3,0.6,0.9", step=1)
buffer = input.float(0.05, title="CMF Buffer", step=0.01)  // 设置CMF的缓冲区范围，防止0轴附近的噪音信号
firstSell = input.float(20, title="sellPrecent1", step=1)
secondSell = input.float(40, title="sellPrecent2", step=1)

// 止损比例参数
stopLossPercentage = input.float(2, title="Stop Loss Percentage", step=1)  // 止损比例，默认2%

// 根据ATR动态调整卖出条件
upPercentage5 = 1.03 + (sellPrecent / 100)  // 动态调整的上涨幅度 3% 基数
upPercentage10 = 1.06 + (sellPrecent / 100)  // 动态调整的上涨 6% 基数
upPercentage15 = 1.09 + (sellPrecent/ 100)  // 动态调整的上涨 9% 基数

// 获取买入价格
var float entryPrice = na

// === CMF优化指标系统 START ===
// Chaikin Money Flow (CMF) 基础计算
lengthCMF = input.int(20, title="cmfLength", minval=1)
ad = close == high and close == low or high == low ? 0 : ((2 * close - low - high) / (high - low)) * volume
mf = math.sum(ad, lengthCMF) / math.sum(volume, lengthCMF)

// CMF强势区域参数
cmfStrengthPeriod = input.int(5, title="CMF强势确认周期", minval=1, maxval=20, group="CMF强势区域")
cmfStrengthThreshold = input.float(0.1, title="CMF强势阈值", step=0.01, group="CMF强势区域")
useCMFStrengthFilter = input.bool(false, title="启用CMF强势区域过滤", group="CMF强势区域")
cmfConsistencyRatio = input.float(0.7, title="强势持续比例", minval=0.5, maxval=1.0, step=0.1, group="CMF强势区域")

// CMF多维度强势判定
// 1. 基础强势：CMF持续在正值区域
cmfBullishZone = mf > buffer

// 2. 强势确认：CMF不仅为正，且有一定强度
cmfStrongBullish = mf > cmfStrengthThreshold

// 3. 持续强势：在指定周期内大部分时间处于强势
cmfConsistentStrength = math.sum(cmfBullishZone ? 1 : 0, cmfStrengthPeriod) >= math.round(cmfStrengthPeriod * cmfConsistencyRatio)

// 4. 趋势强势：CMF呈上升趋势
cmfMA = ta.sma(mf, 3)
cmfTrendUp = cmfMA > cmfMA[1]

// 5. 综合强势指标（核心强势区域判定）
cmfInStrengthZone = cmfBullishZone and cmfConsistentStrength and (cmfStrongBullish or cmfTrendUp)

// 6. 弱势区域警告
cmfWeakZone = mf < -cmfStrengthThreshold and math.sum(mf < -buffer ? 1 : 0, cmfStrengthPeriod) >= 3

// CMF穿越信号
buyCMF = ta.crossover(mf, buffer)      // 瞬时资金流入信号
sellCMF = ta.crossunder(mf, -buffer)  // 瞬时资金流出信号

// CMF可视化
bgcolor(cmfInStrengthZone ? color.new(color.blue, 90) : cmfWeakZone ? color.new(color.red, 95) : na, title="CMF强势/弱势区域")
plot(mf, color=color.purple, linewidth=2, title="CMF")
hline(0, "CMF零轴", color=color.gray, linestyle=hline.style_dashed)
hline(cmfStrengthThreshold, "强势阈值", color=color.blue, linestyle=hline.style_dotted)
hline(-cmfStrengthThreshold, "弱势阈值", color=color.red, linestyle=hline.style_dotted)
hline(buffer, "买入缓冲", color=color.green, linestyle=hline.style_dotted, linewidth=1)
hline(-buffer, "卖出缓冲", color=color.red, linestyle=hline.style_dotted, linewidth=1)
// === CMF优化指标系统 END ===

// 已卖出比例变量
var float totalSoldPercentage = 0.0

// === 优化后的买入信号逻辑 ===
// 在CMF强势区域内触发买入
strongZoneBuySignal = cmfInStrengthZone

// 基础买入信号：CMF上穿buffer
condition1 = buyCMF

// 弱势区域限制（在CMF弱势区域减少买入）
restrictedBuyZone = cmfWeakZone and useCMFStrengthFilter

// 最终买入信号整合
buySignal = (condition1 or (useCMFStrengthFilter ? strongZoneBuySignal : false)) and not restrictedBuyZone

// 强势区域买入信号标记
plotshape(useCMFStrengthFilter and strongZoneBuySignal, title="CMF强势区域买入", 
          location=location.belowbar, style=shape.diamond, 
          color=color.yellow, size=size.tiny)

// 构建买入消息
buyMsg = "🚀 买入信号触发\n"
buyMsg := buyMsg + "━━━━━━━━━━━━━━━━\n"
buyMsg := buyMsg + "🎫 标的: " + syminfo.ticker + "\n"
buyMsg := buyMsg + "💲 价格: " + str.tostring(close, "#.##") + "\n"
buyMsg := buyMsg + "📊 CMF值: " + str.tostring(mf, "#.###") + "\n"
buyMsg := buyMsg + "🛡️ 止损位: " + str.tostring(close * (1 - stopLossPercentage/100), "#.##") + " (-" + str.tostring(stopLossPercentage) + "%)\n"
buyMsg := buyMsg + "━━━━━━━━━━━━━━━━"

// Add debug info if filter is on
if useCMFStrengthFilter
    buyMsg := buyMsg + "\nDetails: StrongZone=" + str.tostring(cmfInStrengthZone)

if (buySignal)
    strategy.entry("Buy", strategy.long, alert_message=buyMsg)
    entryPrice := close
    totalSoldPercentage := 0.0
    alert(buyMsg, alert.freq_once_per_bar)

// 动态调整的卖出条件
sellCondition5 = close >= entryPrice * upPercentage5
sellCondition10 = close >= entryPrice * upPercentage10
sellCondition15 = close >= entryPrice * upPercentage15

// 整合卖出信号
sellAlert = sellCMF

// 注意：为了仅在订单执行时触发卖出提醒，此处移除了sellCMF的显式alert调用。
// 如果希望 sellCMF 触发提醒，建议将其加入平仓条件并使用 alert_message。

// 止损逻辑
stopLossCondition = close <= entryPrice * (1 - stopLossPercentage / 100)

// 构建止损消息
slMsg = "🛑 止损触发\n"
slMsg := slMsg + "━━━━━━━━━━━━━━━━\n"
slMsg := slMsg + "🎫 标的: " + syminfo.ticker + "\n"
slMsg := slMsg + "💲 卖出价: " + str.tostring(close, "#.##") + "\n"
slMsg := slMsg + "📉 亏损幅度: " + str.tostring(stopLossPercentage) + "%\n"
slMsg := slMsg + "━━━━━━━━━━━━━━━━"

if (stopLossCondition and strategy.position_size > 0)
    strategy.close("Buy", alert_message=slMsg)
    totalSoldPercentage := 100.0

// 分阶段卖出
var array<float> sellLevels = array.from(upPercentage5, upPercentage10, upPercentage15)
var array<float> sellPercents = array.from(firstSell, secondSell, 100 - firstSell - secondSell)

for i = 0 to 2
    if close >= entryPrice * array.get(sellLevels, i) and totalSoldPercentage < 100
        sellQty = math.min(array.get(sellPercents, i), 100 - totalSoldPercentage)
        
        // 计算相关数据
        sellPrice = close
        profitPercent = ((sellPrice - entryPrice) / entryPrice) * 100
        remainingPercent = 100 - (totalSoldPercentage + sellQty)
        
        // 构建提醒消息
        msg = "🔔 分批卖出 #" + str.tostring(i + 1) + "\n"
        msg := msg + "━━━━━━━━━━━━━━━━\n"
        msg := msg + "💰 卖出价格: $" + str.tostring(sellPrice, "#.##") + "\n"
        msg := msg + "📈 涨幅: " + str.tostring(profitPercent, "#.##") + "%\n"
        msg := msg + "📊 本次卖出: " + str.tostring(sellQty, "#.##") + "%\n"
        msg := msg + "💼 剩余仓位: " + str.tostring(remainingPercent, "#.##") + "%\n"
        msg := msg + "━━━━━━━━━━━━━━━━"
        
        strategy.close("Buy", qty_percent=sellQty, alert_message=msg)
        totalSoldPercentage += sellQty

// CMF强势区域警报
alertcondition(cmfInStrengthZone, title="CMF进入强势区域", message="CMF Entered Strength Zone - Enhanced Buy Signals Active")
alertcondition(cmfWeakZone, title="CMF进入弱势区域", message="CMF Entered Weak Zone - Buy Signals Restricted")
